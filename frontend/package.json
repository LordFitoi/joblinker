{
  "private": true,
  "scripts": {
    "build": "NODE_ENV=production nuxt build",
    "dev": "NODE_ENV=development nuxt dev",
    "generate": "NODE_ENV=production nuxt generate && nuxi clean",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.2.0",
    "nuxt-gtag": "^0.5.7"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.6",
    "@vuelidate/core": "^2.0.0",
    "@vuelidate/validators": "^2.0.0",
    "axios": "^1.3.0",
    "pinia": "^2.0.29",
    "sass": "^1.57.1"
  }
}
